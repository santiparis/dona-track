<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://java.sun.com/xml/ns/persistence
    http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd"
             version="2.0">

  <!-- HSQLDB en memoria, solo para los tests: se crea y se borra con el proceso,
       asi cada corrida arranca limpia y no toca la base de PostgreSQL -->
  <persistence-unit name="simple-persistence-unit" transaction-type="RESOURCE_LOCAL">
    <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

    <!-- la autodeteccion solo busca entidades junto a este archivo (target/test-classes),
         y el dominio compila en target/classes: por eso hay que listarlas -->
    <class>logistica.domain.Camion</class>
    <class>logistica.domain.Coordenadas</class>
    <class>logistica.domain.DonacionEncolada</class>
    <class>logistica.domain.Entrega</class>
    <class>logistica.domain.Ruta</class>

    <properties>
      <property name="hibernate.archive.autodetection" value="class"/>

      <property name="hibernate.connection.driver_class" value="org.hsqldb.jdbcDriver"/>
      <property name="hibernate.connection.url" value="jdbc:hsqldb:mem:app-db"/>
      <property name="hibernate.connection.username" value="sa"/>
      <property name="hibernate.connection.password" value=""/>
      <property name="hibernate.dialect" value="org.hibernate.dialect.HSQLDialect"/>

      <property name="hibernate.hbm2ddl.auto" value="create-drop"/>
    </properties>

  </persistence-unit>

</persistence>
